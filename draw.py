import sys
import xml.etree.ElementTree as ET

def draw_points(width, height, path_d):
    grid = [[' ' for _ in range(width)] for _ in range(height)]
    # This is too complex to parse and draw bezier in ascii quickly
    pass
